<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.media.router.cts.provider.per_app"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL"/>

    <application android:testOnly="true">
        <uses-library android:name="android.test.runner"/>

        <activity
            android:name="android.media.cts.app.common.ScreenOnActivity"
            android:exported="false"
            android:turnScreenOn="true" />

        <service android:name="android.media.router.cts.provider.PerAppProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.MediaRoute2ProviderService"/>
            </intent-filter>
        </service>
    </application>
    <instrumentation android:name="androidx.test.runner.AndroidJUnitRunner"
        android:targetPackage="android.media.router.cts.provider.per_app"
        android:label="App with MediaRoute2ProviderService for CTS tests."/>
</manifest>