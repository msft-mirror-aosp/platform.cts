<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.provider.cts.contacts.sessionprovider.client">

    <application>
        <receiver
            android:name=".QueryReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.provider.cts.contacts.sessionprovider.client.ACTION_QUERY" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
